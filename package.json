{
  "name": "nuxt-app",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "wrangler d1 migrations apply DB --local --persist-to .wrangler/state && nuxt dev",
    "dev:pages": "pnpm run dev",
    "dev-mac": "TMPDIR=/tmp nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "preview:pages": "wrangler d1 migrations apply DB --local --persist-to .wrangler/state && nuxt build && wrangler pages dev dist --compatibility-date=2026-02-26 --persist-to .wrangler/state",
    "postinstall": "nuxt prepare",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@headlessui/vue": "^1.7.23",
    "@heroicons/vue": "^2.2.0",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "nuxt": "^4.4.7",
    "vue": "^3.5.17",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@types/papaparse": "^5.5.2",
    "nuxi": "^3.25.1",
    "prettier": "^3.9.4",
    "wrangler": "^4.68.1"
  },
  "packageManager": "pnpm@11.9.0"
}
